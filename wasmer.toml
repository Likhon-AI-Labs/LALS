[package]
name = "rudushi4/lals-multi-api-gateway"
version = "2.0.5"
description = "LALS Multi-API Gateway v2.0.5 with lazy model loading, improved error handling, and dual protocol support"
host = "0.0.0.0"
port = 8080

[deployment]
deployment = "wasmer-edge"

[build]
command = "pip install -r requirements.txt"
