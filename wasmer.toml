[package]
name = "rudushi4/lals-multi-api-gateway"
version = "2.0.1"
description = "LALS Multi-API Gateway - Dual protocol support for OpenAI and Anthropic with automatic model download"
host = "0.0.0.0"
port = 8080

[deployment]
deployment = "wasmer-edge"

[build]
command = "pip install -r requirements.txt"
